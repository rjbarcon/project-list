import React from "react";
import { ProjectMain } from "@/library/components";

const Home: React.FC = async () => {
  return (
    <main className="p-4">
      <ProjectMain />
    </main>
  );
};

export default Home;
