import React from "react";
import { ProjectCard } from "@/library/components";

const Home: React.FC = async () => {
  return (
    <main className="p-4">
      <ProjectCard />
    </main>
  );
};

export default Home;
